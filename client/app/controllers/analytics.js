'use strict';

var controllers = angular.module('lunchControllers');

controllers.controller('analyticsController', [ '$scope',
  function($scope) {

    $scope.initialize = function() {
    }

    $scope.initialize();
  }
]);
